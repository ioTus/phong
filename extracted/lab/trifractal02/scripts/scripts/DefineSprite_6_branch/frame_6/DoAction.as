pLitGummy = _parent.pLitGummy;
baseScale = _parent.pLitGummy;
if(1.5 < pLitGummy)
{
   this._rotation += _xmouse / ((80 + oRot / 2) * 2);
}
else
{
   this._rotation += _xmouse / ((80 + oRot / 2) * 2);
}
i++;
